services:
  php:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: halfyshop-php
    volumes:
      - ./app:/var/www/html
    networks:
      - app
    depends_on:
      postgres:
        condition: service_healthy

  php-cli:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: halfyshop-php-cli
    command: ["tail", "-f", "/dev/null"]
    volumes:
      - ./app:/var/www/html
    networks:
      - app
    depends_on:
      postgres:
        condition: service_healthy

  nginx:
    image: nginx:alpine
    container_name: halfyshop-nginx
    ports:
      - "8081:80"
    volumes:
      - ./app:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - app
    depends_on:
      - php

  postgres:
    image: postgres:16-alpine
    container_name: halfyshop-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secret}
      POSTGRES_DB: ${POSTGRES_DB:-halfyshop}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    networks:
      - app
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-app} -d ${POSTGRES_DB:-halfyshop}"]
      interval: 5s
      timeout: 5s
      retries: 5

  postgres_test:
    image: postgres:16-alpine
    container_name: halfyshop-postgres-test
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secret}
      POSTGRES_DB: ${POSTGRES_DB_TEST:-halfyshop_test}
    volumes:
      - postgres_test_data:/var/lib/postgresql/data
    ports:
      - "5435:5432"
    networks:
      - app
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-app} -d ${POSTGRES_DB_TEST:-halfyshop_test}"]
      interval: 5s
      timeout: 5s
      retries: 5

networks:
  app:
    driver: bridge

volumes:
  postgres_data:
  postgres_test_data:
