<div class="theme-preview">
    <div class="theme-preview__inner">
        <div class="tp-header">
            <span class="tp-logo">Anna Pownall</span>
            <div class="tp-nav">
                <span class="tp-nav__link tp-nav__link--active">Obras</span>
                <span class="tp-nav__link">Blog</span>
                <span class="tp-nav__link">Sobre mí</span>
            </div>
        </div>
        <div class="tp-filters">
            <span class="tp-filter tp-filter--active">Todas</span>
            <span class="tp-filter">grabado</span>
            <span class="tp-filter">ilustración</span>
            <span class="tp-filter">print</span>
        </div>
        <div class="tp-grid">
            <div class="tp-card" style="background:#e8e4de;"></div>
            <div class="tp-card" style="background:#d4cfc8;"></div>
            <div class="tp-card" style="background:#c9c3bb;"></div>
            <div class="tp-card" style="background:#e0dbd3;"></div>
            <div class="tp-card" style="background:#dbd5cc;"></div>
            <div class="tp-card" style="background:#d0cac2;"></div>
        </div>
    </div>
</div>

<style>
.theme-preview {
    width: 100%;
    height: 180px;
    overflow: hidden;
    border-radius: 4px 4px 0 0;
    position: relative;
    background: #ffffff;
}
.theme-preview__inner {
    width: 620px;
    transform: scale(0.42);
    transform-origin: top left;
    pointer-events: none;
    font-family: Georgia, serif;
}
.tp-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px 24px;
    border-bottom: 1px solid #babecb;
    background: rgba(255,255,255,0.92);
}
.tp-logo {
    font-family: Georgia, serif;
    font-size: 18px;
    font-weight: 700;
    letter-spacing: 0.05em;
    color: #313339;
}
.tp-nav { display: flex; gap: 28px; }
.tp-nav__link {
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #a5a8b2;
    padding-bottom: 2px;
    border-bottom: 1px solid transparent;
}
.tp-nav__link--active {
    color: #313339;
    border-bottom-color: #313339;
}
.tp-filters {
    display: flex;
    gap: 20px;
    padding: 20px 24px 0;
}
.tp-filter {
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #a5a8b2;
    border-bottom: 1px solid transparent;
}
.tp-filter--active {
    color: #313339;
    border-bottom-color: #313339;
}
.tp-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    padding: 20px 24px;
}
.tp-card {
    aspect-ratio: 3/4;
    border-radius: 2px;
}
</style>
