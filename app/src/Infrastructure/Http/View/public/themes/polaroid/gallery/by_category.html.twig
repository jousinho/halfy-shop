{% extends 'public/themes/polaroid/layout.html.twig' %}

{% block title %}{{ activeCategory.name.value }} — Anna Pownall{% endblock %}

{% block content %}
    {% if categories is not empty %}
    <nav class="pk-filters">
        <a href="{{ path('home') }}" class="pk-filter">{{ 'filter.all'|trans }}</a>
        {% for cat in categories %}
        <a href="{{ path('gallery_by_category', {slug: cat.slug.value}) }}"
           class="pk-filter {% if cat.id.value == activeCategory.id.value %}pk-filter--active{% endif %}">
            {{ cat.name.value }}
        </a>
        {% endfor %}
    </nav>
    <select class="pk-filters-select" onchange="window.location=this.value">
        <option value="{{ path('home') }}">{{ 'filter.all_categories'|trans }}</option>
        {% for cat in categories %}
        <option value="{{ path('gallery_by_category', {slug: cat.slug.value}) }}"
                {% if cat.id.value == activeCategory.id.value %}selected{% endif %}>
            {{ cat.name.value }}
        </option>
        {% endfor %}
    </select>
    {% endif %}

    <div class="pk-grid">
        {% for artwork in artworks %}
            {% include 'public/themes/polaroid/home/_artwork_card.html.twig' with {artwork: artwork} %}
        {% else %}
            <p class="pk-empty">{{ 'gallery.empty_category'|trans }}</p>
        {% endfor %}
    </div>
{% endblock %}
